import React from "react";
import { Text, View, StyleSheet } from "react-native";

const Show = (data) => {
    return (
        <View>
                    
                        
                    <Text>{data}</Text>
                    
        </View>
    );
}

export default Show;